{% if type is defined %}
    <button type="{{ type }}" class="button button--{{ modifier | default('') }}">
        {{ title|trans({}) }}
    </button>

{% else %}

    <a href="{{ url }}" class="button button--{{ modifier | default('') }}">{{ title | raw }}</a>

{% endif %}
