<div id="navbar">
    <section class="transparent">
        <ul class="main">
            {% for category in categories %}
                <li><!--<a href="/{{ category['data']['url'] }}">--><a href="{{ url('catalog/index') }}">{{ category['data']['name'] }}</a></li>
            {% endfor %}
        </ul>
    </section>
</div>
