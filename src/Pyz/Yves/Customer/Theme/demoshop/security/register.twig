{% extends "@application/layout/reduced.twig" %}

{% block content %}
    <h1>{{ "customer.register" | trans }}</h1>
    {{ form_start(registerForm) }}
    {{ form_widget(registerForm) }}
    {{ form_end(registerForm) }}
{% endblock %}