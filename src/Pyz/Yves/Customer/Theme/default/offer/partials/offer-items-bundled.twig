<tr>
    <td class="text-left">
        {{ offerItem.bundleProduct.sku }}
    </td>
    <td class="text-left">
        {{ offerItem.bundleProduct.name }}
    </td>
    <td class="text-right">
        {{ offerItem.bundleProduct.unitPrice | money(true, offer.quote.currency.code) }}
    </td>
    <td class="text-right">
        {{ offerItem.bundleProduct.quantity }}
    </td>
    <td class="text-right">
        {{ offerItem.bundleProduct.unitPriceToPayAggregation | money(true, offer.quote.currency.code) }}
    </td>
</tr>

<tr style="background-color:unset;">
    <td colspan="5">
        {{ "customer.order.bundled_items" | trans }} <br />
        {% for bundledItem in offerItem.bundleItems %}
            - {{ bundledItem.quantity }} x {{ bundledItem.name }} <br />
        {% endfor %}

        {% if offerItem.bundleProduct.productOptions|length > 0 %}
            {{ "customer.order.options" | trans }} <br />
            {% for productOption in offerItem.bundleProduct.productOptions %}
                &nbsp; - {{ productOption.value | trans }} {{ productOption.unitPrice | money(true, offer.quote.currency.code) }}  <br />
            {% endfor %}
        {% endif %}

    </td>
</tr>
