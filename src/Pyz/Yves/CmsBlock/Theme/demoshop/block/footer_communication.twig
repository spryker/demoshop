{%
    set data = {
        channels: [
            {
                headline: 'Beratungs-Telefon',
                icon: 'call',
                link: {
                    title: '+44 191 231 3983',
                    url: 'tel:0441912313983'
                }
            },
            {
                headline: 'Mail',
                icon: 'mail',
                link: {
                    title: 'office@petsdeli.de',
                }
            },
            {
                headline: 'Chat',
                icon: 'chat',
                link: {
                    title: 'Sprechen sie mit uns',
                }
            },
            {
                headline: 'Ladengeschäfte',
                icon: 'store',
                link: {
                    title: 'Unsere Ladengeschäfte',
                }
            },
        ]
    }
%}


<div class="communication">
    <div class="communication__channels">

        {% for channel in data.channels %}
        <div class="communication__channel communication__channel--{{ channel.icon }}">
            <h2 class="communication__label">{{ channel.headline }}</h2>

            <a {% if channel.link.url is defined %} href="{{ channel.link.url }}" {% endif %} class="communication__info">
                {{ channel.link.title }}
            </a>
        </div>
        {% endfor %}

    </div>
</div>

