{% extends "@checkout/layout.twig" %}
{% import "@checkout/checkout/_macros.twig" as checkout %}

{% block title %}
    <h3>{{ 'checkout.step.shipment.title' | trans }}</h3>
{% endblock %}

{% block content %}
    <div class="row columns">

        {{ form_start(shipmentForm, { attr: { id: 'payment-form' }}) }}

            <div class="callout">
                <ul class="no-bullet">

                      {% for name, choices in shipmentForm.idShipmentMethod.vars.choices %}

                          <h4>{{ name }}</h4>

                          {% for key, choice in choices %}
                            <li>
                                <label>
                                   &nbsp; {{ form_widget(shipmentForm.idShipmentMethod[key], {'attr': {'class': '__toggler'}}) }}
                                    {{ shipmentForm.idShipmentMethod[key].vars.label | raw }}
                                </label>
                            </li>
                          {% endfor %}
                    {% endfor %}
                </ul>
            </div>

            <div class="callout">
                <div class="row align-right">
                    <div class="small-12 medium-6 large-4 xlarge-3 columns">
                        <a href="{{ previousStepUrl }}" class="button expanded __no-margin-bottom">{{ 'general.back.button' | trans }}</a>
                    </div>
                    <div class="small-12 medium-6 large-4 xlarge-3 columns">
                        <button type="submit" class="button success expanded __no-margin-bottom">{{ 'general.next.button' | trans }}</button>
                    </div>
                </div>
            </div>

        {{ form_end(shipmentForm) }}
    </div>
{% endblock %}
