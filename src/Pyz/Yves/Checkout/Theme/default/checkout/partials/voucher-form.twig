{{ form_start(voucherForm) }}
<div class="callout">
    <div class="row">
        <div class="columns">
            <h5>{{ voucherForm.voucherDiscounts.vars.label | trans }}</h5>
            <div class="input-group">
                {{ form_widget(voucherForm.voucherDiscounts, {attr: {class: 'input-group-field'}}) }}
                <div class="input-group-button">
                    <button class="button expanded info" type="submit"><i class="fa fa-gift"></i>{{ 'cart.voucher.redeem' | trans }}</button>
                </div>
            </div>
            {{ form_errors(voucherForm.voucherDiscounts) }}
        </div>
    </div>
</div>
{{ form_end(voucherForm) }}
