Index: vendor/spryker/spryker/Bundles/Cart/src/SprykerFeature/Client/Cart/Service/CartClient.php
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- vendor/spryker/spryker/Bundles/Cart/src/SprykerFeature/Client/Cart/Service/CartClient.php	(revision a8d4b83f77fb0974368e5f6fde10ca65bbe9343e)
+++ vendor/spryker/spryker/Bundles/Cart/src/SprykerFeature/Client/Cart/Service/CartClient.php	(revision )
@@ -45,8 +45,7 @@
 
         $this->getSession()
             ->setItemCount(0)
-            ->setCart($cartTransfer)
-        ;
+            ->setCart($cartTransfer);
 
         return $cartTransfer;
     }
@@ -132,10 +131,15 @@
         }
 
         $itemTransfer = $this->findItem($itemTransfer);
+
+        $delta = abs($itemTransfer->getQuantity() - $quantity);
+
+        if ($delta !== 0) {
-        if ($itemTransfer->getQuantity() > $quantity) {
+            if ($itemTransfer->getQuantity() > $quantity) {
-            return $this->decreaseItemQuantity($itemTransfer, $quantity);
+                return $this->decreaseItemQuantity($itemTransfer, $delta);
-        } else {
+            } else {
-            return $this->increaseItemQuantity($itemTransfer, $quantity);
+                return $this->increaseItemQuantity($itemTransfer, $delta);
+            }
         }
     }
 
